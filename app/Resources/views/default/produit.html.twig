{% extends 'base1.html.twig' %}

{% block body %}
    
    <section>
        <div class="container">
            <div class="row">
                <div class="col-sm-3">
                    <div class="left-sidebar">
                        <h2>{{ categorie.libelle }}</h2>
                        <div class="panel-group category-products" id="accordian">
                            {{ categorie.description }}
                        </div>
                        

                        
                        
                    </div>
                </div>
                
                <div class="col-sm-9 padding-right">
                    <div class="features_items"><!--features_items-->
                        <h2 class="title text-center">Les produits de la catégorie {{ categorie.libelle }}</h2>
                        
                        {% for produit in produits %}
                        <div class="col-sm-4">
                            <div class="product-image-wrapper">
                                <div class="single-products">
                                    
                                        <div>
                                         <h2>{{produit.libelle}}</h2>
                                         <p>{{produit.desccourt}}</p>            
                                          <img src="{{produit.imageprinc}}" class="rounded" alt="Cinque Terre" width="304" height="236">
                                          <div lass="col-sm-9 padding-right">
                                          <a href="{{ path('details_produit',{ 'id' : produit.id})}}" >
                                          <button type="button" class="btn btn-info">Details</button>
                                          </a> 
                                        </div>
                                        </div>
                                                                        
                                </div>
                            </div>
                        </div>


                        
                        {% endfor %}

                        
                    </div><!--features_items-->
                    
                </div>
            </div>
        </div>
    </section>
    {% endblock %}